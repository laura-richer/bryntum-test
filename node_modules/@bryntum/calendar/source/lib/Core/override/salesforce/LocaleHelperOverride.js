import Override from '../../../Core/mixin/Override.js';
import LocaleHelper from '../../localization/LocaleHelper.js';

class LocaleHelperOverride {

    static target = {
        class : LocaleHelper
    };


    static get global() {
        return self;
    }
}

Override.apply(LocaleHelperOverride);
