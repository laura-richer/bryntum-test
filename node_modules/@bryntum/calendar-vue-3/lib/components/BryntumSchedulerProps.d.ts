import type { AjaxStore, AjaxStoreConfig, AssignmentModel, AssignmentModelConfig, AssignmentStore, AssignmentStoreConfig, CellCopyPaste, CellCopyPasteConfig, CellEdit, CellEditConfig, CellMenu, CellMenuConfig, CellTooltip, CellTooltipConfig, ColumnAutoWidth, ColumnAutoWidthConfig, ColumnDragToolbar, ColumnDragToolbarConfig, ColumnLines, ColumnLinesConfig, ColumnPicker, ColumnPickerConfig, ColumnRename, ColumnRenameConfig, ColumnReorder, ColumnReorderConfig, ColumnResize, ColumnResizeConfig, ColumnStore, ColumnStoreConfig, ContainerItemConfig, CrudManager, CrudManagerConfig, DateConstraint, Dependencies, DependenciesConfig, DependencyBaseModel, DependencyEdit, DependencyEditConfig, DependencyMenu, DependencyMenuConfig, DependencyModel, DependencyModelConfig, DependencyStore, DependencyStoreConfig, DomClassList, DomConfig, DurationConfig, DurationUnit, EventColor, EventDrag, EventDragConfig, EventDragCreate, EventDragCreateConfig, EventDragSelect, EventDragSelectConfig, EventFilter, EventFilterConfig, EventModel, EventModelConfig, EventNonWorkingTime, EventNonWorkingTimeConfig, EventResize, EventResizeConfig, EventStore, EventStoreConfig, FileDrop, FileDropConfig, FillHandle, FillHandleConfig, Filter, FilterBar, FilterBarConfig, FilterConfig, FormulaProviderConfig, GridColumnConfig, GridSelectionMode, Group, GroupConfig, GroupSummary, GroupSummaryConfig, HeaderMenu, HeaderMenuConfig, HeaderZoom, HeaderZoomConfig, KeyMapConfig, Labels, LabelsConfig, LockRows, LockRowsConfig, Mask, MaskConfig, MenuConfig, MergeCells, MergeCellsConfig, Model, ModelConfig, NonWorkingTime, NonWorkingTimeConfig, PagingToolbarConfig, Pan, PanConfig, PanelCollapserConfig, PanelCollapserOverlayConfig, PanelHeader, PdfExport, PdfExportConfig, PreserveScrollOptions, ProjectModel, ProjectModelConfig, QuickFind, QuickFindConfig, RecurrenceConfirmationPopup, RecurrenceConfirmationPopupConfig, RegionResize, RegionResizeConfig, ResourceHeaderConfig, ResourceMarginConfig, ResourceMenu, ResourceMenuConfig, ResourceModel, ResourceModelConfig, ResourceStore, ResourceStoreConfig, ResourceTimeRangeModel, ResourceTimeRangeModelConfig, ResourceTimeRangeStore, ResourceTimeRangeStoreConfig, ResourceTimeRanges, ResourceTimeRangesConfig, RowCopyPaste, RowCopyPasteConfig, RowEdit, RowEditConfig, RowExpander, RowExpanderConfig, RowReorder, RowReorderConfig, RowResize, RowResizeConfig, ScheduleContext, ScheduleContextConfig, Scheduler, SchedulerCrudManager, SchedulerEventCopyPaste, SchedulerEventCopyPasteConfig, SchedulerEventEdit, SchedulerEventEditConfig, SchedulerEventMenu, SchedulerEventMenuConfig, SchedulerEventTooltip, SchedulerEventTooltipConfig, SchedulerExcelExporter, SchedulerExcelExporterConfig, SchedulerListeners, SchedulerPrint, SchedulerPrintConfig, SchedulerScheduleMenu, SchedulerScheduleMenuConfig, SchedulerScheduleTooltip, SchedulerScheduleTooltipConfig, SchedulerTimeRanges, SchedulerTimeRangesConfig, ScrollButtons, ScrollButtonsConfig, ScrollManager, ScrollManagerConfig, Scroller, ScrollerConfig, Search, SearchConfig, SimpleEventEdit, SimpleEventEditConfig, Sort, SortConfig, Split, SplitConfig, StateProvider, StickyCells, StickyCellsConfig, StickyEvents, StickyEventsConfig, Store, StoreConfig, Stripe, StripeConfig, SubGridConfig, Summary, SummaryConfig, TabConfig, TimeAxis, TimeAxisConfig, TimeAxisHeaderMenu, TimeAxisHeaderMenuConfig, TimeSelection, TimeSelectionConfig, TimeSpan, TimeSpanConfig, TimelineBase, ToolConfig, ToolbarConfig, ToolbarItems, TooltipConfig, Tree, TreeConfig, TreeGroup, TreeGroupConfig, TreeSummary, TreeSummaryConfig, VerticalTimeAxisColumnConfig, ViewPresetConfig, VisibleDate, Widget } from '@bryntum/calendar';
export declare type BryntumSchedulerProps = {
    /**
     * Element (or element id) to adopt as this Widget's encapsulating element. The widget's
     * content will be placed inside this element.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-adopt)
     */
    adopt?: HTMLElement | string;
    /**
     * When this widget is a child of a [Container](https://bryntum.com/products/calendar/docs/api/Core/widget/Container), it will by default be participating in a
     * flexbox layout. This config allows you to set this widget's
     * [align-self](https://developer.mozilla.org/en-US/docs/Web/CSS/align-self) style.
     */
    alignSelf?: string;
    /**
     * Set to `false` to not allow creating dependencies
     */
    allowCreate?: Boolean;
    /**
     * Set it to `true` to allow dependency creation only for parent events (only applies to Scheduler Pro using the `NestedEvents` feature). Normally the nested event container
     * inside parent events cannot be scrolled when using dependencies, but by enabling this setting and limiting
     * to where dependencies can be drawn scrolling will be enabled.
     */
    allowCreateOnlyParent?: Boolean;
    /**
     * `false` to require a drop on a target event bar side circle to define the dependency type.
     * If dropped on the event bar, the `defaultValue` of the DependencyModel `type` field will be used to
     * determine the target task side.
     */
    allowDropOnEventBar?: Boolean;
    /**
     * Set to `false` if you don't want to allow events overlapping times for any one resource (defaults to `true`).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-allowOverlap)
     */
    allowOverlap?: Boolean;
    /**
     * Set to `true` to animate row removals caused by filtering.
     */
    animateFilterRemovals?: Boolean;
    /**
     * Set to `false` to prevent remove row animation and remove the delay related to that.
     */
    animateRemovingRows?: Boolean;
    /**
     * When the [Tree](https://bryntum.com/products/calendar/docs/api/Grid/feature/Tree) feature is in use and the Store is a tree store, this
     * config may be set to `true` to visually animate branch node expand and collapse operations.
     * *This is not supported in Scheduler and Gantt*
     */
    animateTreeNodeToggle?: Boolean;
    /**
     * Element (or the id of an element) to append this widget's element to. Can be configured, or set once at
     * runtime. To access the element of a rendered widget, see [element](https://bryntum.com/products/calendar/docs/api/Core/widget/Widget#property-element).
     */
    appendTo?: HTMLElement | string;
    /**
     * A localizable string (May contain `'L{}'` tokens which resolve in the locale file) to inject
     * into an element which will be linked using the `aria-describedby` attribute.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-ariaDescription)
     */
    ariaDescription?: string;
    /**
     * A localizable string (May contain `'L{}'` tokens which resolve in the locale file) to inject as
     * the `aria-label` attribute.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-ariaLabel)
     */
    ariaLabel?: string;
    /**
     * The `aria-live` value for this widget.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-ariaLive)
     * @deprecated Will be removed in 7.0
     */
    ariaLive?: string;
    /**
     * Inline assignments, will be loaded into an internally created AssignmentStore.
     */
    assignments?: AssignmentModel[] | object[] | AssignmentModelConfig[];
    /**
     * The optional [AssignmentStore](https://bryntum.com/products/calendar/docs/api/Scheduler/data/AssignmentStore), holding assignments between resources and events.
     * Required for multi assignments.
     */
    assignmentStore?: AssignmentStore | AssignmentStoreConfig;
    /**
     * You can set this option to `false` to make the timeline panel start and end on the exact provided
     * [startDate](https://bryntum.com/products/calendar/docs/api/Scheduler/view/TimelineBase#config-startDate)/[endDate](#Scheduler/view/TimelineBase#config-endDate) w/o adjusting them.
     */
    autoAdjustTimeAxis?: Boolean;
    /**
     * If this config is set, then its `gesture` property (which defaults to `dblclick`) creates a
     * new event at the mouse or touch event's time point.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-autoCreate)
     * @param {'click','dblclick'} autoCreate.gesture The gesture to trigger auto creation of an event. Defaults to `dblclick`.
     * @param {boolean} autoCreate.useEventModelDefaults Set to `true` to set default duration based on the defaults specified by the [duration](https://bryntum.com/products/calendar/docs/api/Scheduler/model/EventModel#field-duration) and [durationUnit](https://bryntum.com/products/calendar/docs/api/Scheduler/model/EventModel#field-durationUnit) fields.
     * @param {'round','ceil','floor'} autoCreate.snapType The type of snapping to apply to the event start date. If omitted, the date will be floored to the time axis resolution.
     * @param {DurationConfig,string} autoCreate.step The increment to snap the event start date to.
     * @param {DurationConfig,string} autoCreate.duration The duration of the event to create. Defaults to 1 time axis tick.
     * @param {string} autoCreate.name The name of the event to create. Defaults to the `Object.newEvent` property from the locale.
     */
    autoCreate?: Boolean | string | {
        autoCreate: {
            gesture?: 'click' | 'dblclick';
            useEventModelDefaults?: Boolean;
            snapType?: 'round' | 'ceil' | 'floor';
            step?: DurationConfig | string;
            duration?: DurationConfig | string;
            name?: string;
        };
    };
    /**
     * Automatically set grids height to fit all rows (no scrolling in the grid). In general you should avoid
     * using `autoHeight: true`, since it will bypass Grids virtual rendering and render all rows at once, which
     * in a larger grid is really bad for performance.
     */
    autoHeight?: Boolean;
    /**
     * Controls how much space to leave between stacked event bars in px.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-barMargin)
     */
    barMargin?: number;
    /**
     * A Config object representing the configuration of a [Toolbar](https://bryntum.com/products/calendar/docs/api/Core/widget/Toolbar),
     * or array of config objects representing the child items of a Toolbar. Another way to add a bbar is to use [strips](https://bryntum.com/products/calendar/docs/api/Core/widget/Panel#config-strips).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-bbar)
     */
    bbar?: (ContainerItemConfig | string)[] | ToolbarConfig | PagingToolbarConfig | null;
    /**
     * Custom CSS classes to add to the panel's body element.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-bodyCls)
     */
    bodyCls?: string | object;
    /**
     * An object where property names with a truthy value indicate which events should bubble up the ownership
     * hierarchy when triggered.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-bubbleEvents)
     */
    bubbleEvents?: object;
    /**
     * This config defines the size of the start and end invisible parts of the timespan when [infiniteScroll](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/TimelineScroll#config-infiniteScroll) set to `true`.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-bufferCoef)
     */
    bufferCoef?: number;
    /**
     * This config defines the scroll limit, which, when exceeded will cause a timespan shift.
     * The limit is calculated as the `panelWidth * [bufferCoef](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/TimelineScroll#config-bufferCoef) * bufferThreshold`. During scrolling, if the left or right side
     * has less than that of the rendered content - a shift is triggered.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-bufferThreshold)
     */
    bufferThreshold?: number;
    /**
     * Set to `false` to not call onXXX method names (e.g. `onShow`, `onClick`), as an easy way to listen for events.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-callOnFunctions)
     */
    callOnFunctions?: Boolean;
    /**
     * Set to `false` to crop text in grid cells without ellipsis (...). When enabled, cells containing pure
     * use `display : block`, instead of `display : flex` to allow ellipsis to work.
     * <strong>NOTE</strong> Only supported in browsers that support `:has()` CSS selector
     */
    cellEllipsis?: Boolean;
    /**
     * Custom CSS classes to add to element.
     * May be specified as a space separated string, or as an object in which property names
     * with truthy values are used as the class names:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-cls)
     */
    cls?: string | object;
    /**
     * Controls whether the panel is collapsed (the body of the panel is hidden while only the header is
     * visible). Only valid if the panel is [collapsible](https://bryntum.com/products/calendar/docs/api/Core/widget/Panel#config-collapsible).
     */
    collapsed?: Boolean;
    /**
     * This config enables collapsibility for the panel. See [collapsed](https://bryntum.com/products/calendar/docs/api/Core/widget/Panel#config-collapsed).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-collapsible)
     */
    collapsible?: Boolean | PanelCollapserConfig | PanelCollapserOverlayConfig;
    /**
     * Set to `false` to not show column lines. End result might be overruled by/differ between themes.
     */
    columnLines?: Boolean;
    /**
     * Accepts column definitions for the grid during initialization. They will be used to create
     * [Column](https://bryntum.com/products/calendar/docs/api/Grid/column/Column) instances that are added to a [ColumnStore](#Grid/data/ColumnStore).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-columns)
     */
    columns?: ColumnStore | GridColumnConfig[] | ColumnStoreConfig;
    config?: object;
    /**
     * Custom CSS classes to add to the [contentElement](https://bryntum.com/products/calendar/docs/api/Core/widget/Widget#property-contentElement).
     * May be specified as a space separated string, or as an object in which property names
     * with truthy values are used as the class names:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-contentElementCls)
     */
    contentElementCls?: string | object;
    /**
     * Event which is used to show context menus.
     * Available options are: 'contextmenu', 'click', 'dblclick'.
     */
    contextMenuTriggerEvent?: 'contextmenu' | 'click' | 'dblclick';
    /**
     * <div class="note">This is deprecated in favor of [autoCreate](https://bryntum.com/products/calendar/docs/api/Scheduler/view/SchedulerBase#config-autoCreate) which allows for more
     * granular control of how a click or double click creates an event.</div>
     * If true, a new event will be created when user double-clicks on a time axis cell (if scheduler is not in
     * read only mode).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-createEventOnDblClick)
     * @param {boolean} createEventOnDblClick.useEventModelDefaults set to `true` to set default duration based on the defaults specified by the [duration](https://bryntum.com/products/calendar/docs/api/Scheduler/model/EventModel#field-duration) and [durationUnit](https://bryntum.com/products/calendar/docs/api/Scheduler/model/EventModel#field-durationUnit) fields.
     * @deprecated 6.1.8 Use [autoCreate](https://bryntum.com/products/calendar/docs/api/Scheduler/view/SchedulerBase#config-autoCreate) instead.
     */
    createEventOnDblClick?: Boolean | {
        createEventOnDblClick: {
            useEventModelDefaults?: Boolean;
        };
    };
    /**
     * A tooltip config object that will be applied to the dependency creation [Tooltip](https://bryntum.com/products/calendar/docs/api/Core/widget/Tooltip)
     */
    creationTooltip?: TooltipConfig;
    /**
     * A template function that will be called to generate the HTML contents of the dependency creation tooltip.
     * You can return either an HTML string or a [DomConfig](https://bryntum.com/products/calendar/docs/api/Core/helper/DomHelper#typedef-DomConfig) object.
     * @param {object} data Data about the dependency being created
     * @param {Scheduler.model.TimeSpan} data.source The from event
     * @param {Scheduler.model.TimeSpan} data.target The target event
     * @param {string} data.fromSide The from side (start, end, top, bottom)
     * @param {string} data.toSide The target side (start, end, top, bottom)
     * @param {boolean} data.valid The validity of the dependency
     * @returns {string,DomConfig}
     */
    creationTooltipTemplate?: (data: {
        source: TimeSpan;
        target: TimeSpan;
        fromSide: string;
        toSide: string;
        valid: Boolean;
    }) => string | DomConfig;
    /**
     * Supply a [CrudManager](https://bryntum.com/products/calendar/docs/api/Scheduler/data/CrudManager) instance or a config object if you want to use
     * CrudManager for handling data.
     */
    crudManager?: CrudManagerConfig | SchedulerCrudManager;
    /**
     * Class that should be used to instantiate a CrudManager in case it's provided as a simple object to
     * [crudManager](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/SchedulerStores#config-crudManager) config.
     */
    crudManagerClass?: typeof CrudManager;
    /**
     * Convenient shortcut to set data in grids store both during initialization and at runtime. Can also be
     * used to retrieve data at runtime, although we do recommend interacting with Grids store instead using
     * the [store](https://bryntum.com/products/calendar/docs/api/Grid/view/GridBase#property-store) property.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-data)
     */
    data?: object[] | Model[] | ModelConfig[];
    /**
     * Object to apply to elements dataset (each key will be used as a data-attribute on the element)
     */
    dataset?: Record<string, string>;
    /**
     * The date to display when used as a component of a Calendar.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-date)
     */
    date?: Date;
    /**
     * A [DateHelper](https://bryntum.com/products/calendar/docs/api/Core/helper/DateHelper) format string to use to create date output for view descriptions.
     */
    dateFormat?: string;
    /**
     * A string used to separate start and end dates in the [descriptionFormat](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/Describable#config-descriptionFormat).
     */
    dateSeparator?: string;
    /**
     * Region to which columns are added when they have none specified
     */
    defaultRegion?: string;
    /**
     * Generic resource image, used when provided `imageUrl` or `image` fields or path calculated from resource
     * name are all invalid. If left blank, resource name initials will be shown when no image can be loaded.
     */
    defaultResourceImageName?: string;
    /**
     * Inline dependencies, will be loaded into an internally created DependencyStore.
     */
    dependencies?: DependencyModel[] | DependencyModelConfig[];
    /**
     * The optional [DependencyStore](https://bryntum.com/products/calendar/docs/api/Scheduler/data/DependencyStore).
     */
    dependencyStore?: DependencyStore | DependencyStoreConfig;
    /**
     * The date format used by the default [descriptionRenderer](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/Describable#config-descriptionRenderer) for rendering the view's description.
     * If this value is `null`, the [dateFormat](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/Describable#config-dateFormat) (and potentially [dateSeparator](#Scheduler/view/mixin/Describable#config-dateSeparator)) will
     * be used.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-descriptionFormat)
     */
    descriptionFormat?: string | string[] | Boolean[];
    /**
     * A function that provides the textual description for this view. If provided, this function overrides the
     * [descriptionFormat](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/Describable#config-descriptionFormat).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-descriptionRenderer)
     * @param {Core.widget.Widget} view The active view in case the function is in another scope.
     * @returns {string} Description string
     */
    descriptionRenderer?: (view: Widget) => string;
    /**
     * Configure as `false` to preserve selection when clicking the empty schedule area.
     */
    deselectAllOnScheduleClick?: Boolean;
    /**
     * Configure as `true` to deselect a selected event upon click.
     */
    deselectOnClick?: Boolean;
    /**
     * Set to `true` to destroy the store when the grid is destroyed.
     */
    destroyStore?: Boolean;
    /**
     * Configure as `true` to destroy the Project and stores when `this` is destroyed.
     */
    destroyStores?: Boolean;
    /**
     * Disable or enable the widget. It is similar to [readOnly](https://bryntum.com/products/calendar/docs/api/Core/widget/Widget#config-readOnly) except a disabled widget
     * cannot be focused, uses a different rendition (usually greyish) and does not allow selecting its value.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-disabled)
     */
    disabled?: Boolean | 'inert';
    /**
     * Set to `true` to not get a warning when calling [getState](https://bryntum.com/products/calendar/docs/api/Grid/view/GridBase#function-getState) when there is a column
     * configured without an `id`. But the recommended action is to always configure columns with an `id` when
     * using states.
     */
    disableGridColumnIdWarning?: Boolean;
    /**
     * Set to `true` to not get a warning when using another base class than GridRowModel for your grid data. If
     * you do, and would like to use the full feature set of the grid then include the fields from GridRowModel
     * in your model definition.
     */
    disableGridRowModelWarning?: Boolean;
    /**
     * Defines how dates will be formatted in tooltips etc. This config has priority over similar config on the
     * view preset. For allowed values see [format](https://bryntum.com/products/calendar/docs/api/Core/helper/DateHelper#function-format-static).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-displayDateFormat)
     */
    displayDateFormat?: string;
    /**
     * Controls the placement of this widget when it is added to a [panel's ](https://bryntum.com/products/calendar/docs/api/Core/widget/Panel)
     * [strips collection](https://bryntum.com/products/calendar/docs/api/Core/widget/Panel#config-strips). Typical values for this config are `'top'`,
     * `'bottom'`, `'left'`, or `'right'`, which cause the widget to be placed on that side of the panel's
     * body. Such widgets are called "edge strips".
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-dock)
     */
    dock?: 'top' | 'bottom' | 'left' | 'right' | 'start' | 'end' | 'header' | 'pre-header' | object;
    /**
     * Make this Panel a docked drawer which slides out from one side of the browser viewport by default.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-drawer)
     * @param {'start','left','end','right','top','bottom'} side The side of the viewport to dock the drawer to.  * `'start'` means the `inline-start` side. * `'end'` means the `inline-end` side.
     * @param {string,number} size The size of the drawer in its collapsible axis.
     * @param {boolean} inline If using the [appendTo](https://bryntum.com/products/calendar/docs/api/Core/widget/Panel#config-appendTo) config to place the drawer inside a host widget, set may be set to `true` to make the drawer inline within that host. Note that, if using this, the layout of the host element must have `flex-direction` set appropriately.
     */
    drawer?: Boolean | {
        side?: 'start' | 'left' | 'end' | 'right' | 'top' | 'bottom';
        size?: string | number;
        inline?: Boolean;
    };
    /**
     * Decimal precision used when displaying durations, used by tooltips and DurationColumn.
     * Specify `false` to use raw value
     */
    durationDisplayPrecision?: number | Boolean;
    /**
     * An object specifying attributes to assign to the root element of this widget.
     * Set `null` value to attribute to remove it.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-elementAttributes)
     */
    elementAttributes?: Record<string, string | null>;
    /**
     * Text or HTML, or a [DomConfig](https://bryntum.com/products/calendar/docs/api/Core/helper/DomHelper#typedef-DomConfig) block to display when there is no data to display in the grid
     */
    emptyText?: string | DomConfig;
    /**
     * Allow using [Delete] and [Backspace] to remove events/assignments
     */
    enableDeleteKey?: Boolean;
    /**
     * Set to `false` if you don't want event bar DOM updates to animate.
     */
    enableEventAnimations?: Boolean;
    /**
     * Enables showing occurrences of recurring events across the scheduler's time axis. If you want to disable
     * the recurrence popup, you can choose set the `defaultAction` to `future` to affect all future
     * occurrences, or `single` to just affect the currently selected event.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-enableRecurringEvents)
     */
    enableRecurringEvents?: Boolean | {
        defaultAction?: 'single' | 'future';
    };
    /**
     * Configure this as `true` to allow elements within cells to be styled as `position: sticky`.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-enableSticky)
     */
    enableSticky?: Boolean;
    /**
     * Set to `true` to allow text selection in the grid cells. Note, this cannot be used simultaneously with the
     * `RowReorder` feature.
     */
    enableTextSelection?: Boolean;
    /**
     * When true, some features will start a project transaction, blocking the project queue, suspending
     * store events and preventing UI from updates. It behaves similar to
     * [instantUpdate](https://bryntum.com/products/calendar/docs/api/Grid/column/Column#config-instantUpdate) set to `false`.
     * Set `false` to not use project queue.
     */
    enableTransactionalFeatures?: Boolean;
    /**
     * Set to `true` to listen for CTRL-Z (CMD-Z on Mac OS) keyboard event and trigger undo (redo when SHIFT is
     * pressed). Only applicable when using a [StateTrackingManager](https://bryntum.com/products/calendar/docs/api/Core/data/stm/StateTrackingManager).
     */
    enableUndoRedoKeys?: Boolean;
    /**
     * The end date of the timeline (if not configure with [infiniteScroll](https://bryntum.com/products/calendar/docs/api/Scheduler/view/TimelineBase#config-infiniteScroll)).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-endDate)
     */
    endDate?: Date | string;
    /**
     * The name of the end date parameter that will be passed to in every `eventStore` load request.
     */
    endParamName?: string;
    /**
     * Field from EventModel displayed as text in the bar when rendering
     */
    eventBarTextField?: string;
    /**
     * Event color used by default. Events and resources can specify their own color, with priority order being:
     * Event -&gt; Resource -&gt; Scheduler default.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-eventColor)
     */
    eventColor?: EventColor;
    /**
     * Defines how to handle overlapping events. Valid values are:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-eventLayout)
     */
    eventLayout?: 'stack' | 'pack' | 'mixed' | 'none' | {
        type?: 'stack' | 'pack' | 'mixed' | 'none';
    };
    /**
     * An advanced setting that controls the maximum number of released event bars to keep in the DOM.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-eventReleaseThreshold)
     */
    eventReleaseThreshold?: number;
    /**
     * An empty function by default, but provided so that you can override it. This function is called each time
     * an event is rendered into the schedule to render the contents of the event. It's called with the event,
     * its resource and a `renderData` object which allows you to populate data placeholders inside the event
     * template.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-eventRenderer)
     * @param {object} detail An object containing the information needed to render an Event.
     * @param {Scheduler.model.EventModel} detail.eventRecord The event record.
     * @param {Scheduler.model.ResourceModel} detail.resourceRecord The resource record.
     * @param {Scheduler.model.AssignmentModel} detail.assignmentRecord The assignment record.
     * @param {Scheduler.view.Scheduler} detail.scheduler The Scheduler instance.
     * @param {object} detail.renderData An object containing details about the event rendering.
     * @param {Scheduler.model.EventModel} detail.renderData.event The event record.
     * @param {Core.helper.util.DomClassList,string} detail.renderData.cls An object whose property names represent the CSS class names to be added to the event bar element. Set a property's value to truthy or falsy to add or remove the class name based on the property name. Using this technique, you do not have to know whether the class is already there, or deal with concatenation.
     * @param {Core.helper.util.DomClassList,string} detail.renderData.wrapperCls An object whose property names represent the CSS class names to be added to the event wrapper element. Set a property's value to truthy or falsy to add or remove the class name based on the property name. Using this technique, you do not have to know whether the class is already there, or deal with concatenation.
     * @param {Core.helper.util.DomClassList,string} detail.renderData.iconCls An object whose property names represent the CSS class names to be added to an event icon element. Note that an element carrying this icon class is injected into the event element *after* the renderer completes, *before* the renderer's created content. To disable this if the renderer takes full control and creates content using the iconCls, you can set `renderData.iconCls = null`.
     * @param {number} detail.renderData.left Vertical offset position (in pixels) on the time axis.
     * @param {number} detail.renderData.width Width in pixels of the event element.
     * @param {number} detail.renderData.height Height in pixels of the event element.
     * @param {string,Record<string, string>} detail.renderData.style Inline styles for the event bar DOM element. Use either 'border: 1px solid black' or `{ border: '1px solid black' }`
     * @param {string,Record<string, string>} detail.renderData.wrapperStyle Inline styles for wrapper of the event bar DOM element. Use either 'border: 1px solid green' or `{ border: '1px solid green' }`
     * @param {string} detail.renderData.eventStyle The `eventStyle` of the event. Use this to apply custom styles to the event DOM element
     * @param {string} detail.renderData.eventColor The `eventColor` of the event. Use this to set a custom color for the rendered event
     * @param {string} detail.renderData.ariaLabel A description of the event details used for screen readers
     * @param {DomConfig[]} detail.renderData.children An array of DOM configs used as children to the `b-sch-event` element. Can be populated with additional DOM configs to have more control over contents.
     * @returns {string,DomConfig,DomConfig[]} A simple string, or a DomConfig (or array thereof)
     */
    eventRenderer?: (detail: {
        eventRecord: EventModel;
        resourceRecord: ResourceModel;
        assignmentRecord: AssignmentModel;
        scheduler: Scheduler;
        renderData: {
            event: EventModel;
            cls: DomClassList | string;
            wrapperCls: DomClassList | string;
            iconCls: DomClassList | string;
            left: number;
            width: number;
            height: number;
            style: string | Record<string, string>;
            wrapperStyle: string | Record<string, string>;
            eventStyle: string;
            eventColor: string;
            ariaLabel: string;
            children: DomConfig[];
        };
    }) => string | DomConfig | DomConfig[];
    /**
     * `this` reference for the [eventRenderer](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/SchedulerEventRendering#config-eventRenderer) function
     */
    eventRendererThisObj?: object;
    /**
     * Inline events, will be loaded into an internally created EventStore.
     */
    events?: EventModel[] | EventModelConfig[];
    /**
     * Configure as `true`, or set property to `true` to disable event selection.
     */
    eventSelectionDisabled?: Boolean;
    /**
     * The [EventStore](https://bryntum.com/products/calendar/docs/api/Scheduler/data/EventStore) holding the events to be rendered into the scheduler (required).
     */
    eventStore?: EventStore | EventStoreConfig;
    /**
     * Event style used by default. Events and resources can specify their own style, with priority order being:
     * Event -&gt; Resource -&gt; Scheduler default. Determines the appearance of the event by assigning a CSS class
     * to it. Available styles are:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-eventStyle)
     */
    eventStyle?: 'plain' | 'border' | 'colored' | 'hollow' | 'line' | 'dashed' | 'minimal' | 'rounded' | 'calendar' | 'interday' | null;
    extraData?: any;
    /**
     * Set to `true` to stretch the last column in a grid with all fixed width columns
     * to fill extra available space if the grid's width is wider than the sum of all
     * configured column widths.
     */
    fillLastColumn?: Boolean;
    /**
     * Specify `true` to force rendered events/tasks to fill entire ticks. This only affects rendering, start
     * and end dates retain their value on the data level.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-fillTicks)
     */
    fillTicks?: Boolean;
    /**
     * Use fixed row height. Setting this to `true` will configure the underlying RowManager to use fixed row
     * height, which sacrifices the ability to use rows with variable height to gain a fraction better
     * performance.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-fixedRowHeight)
     */
    fixedRowHeight?: Boolean;
    /**
     * When this widget is a child of a [Container](https://bryntum.com/products/calendar/docs/api/Core/widget/Container), it will by default be participating in a
     * flexbox layout. This config allows you to set this widget's
     * [flex](https://developer.mozilla.org/en-US/docs/Web/CSS/flex) style.
     * This may be configured as a single number or a `&lt;flex-grow&gt; &lt;flex-shrink&gt; &lt;flex-basis&gt;` format string.
     * numeric-only values are interpreted as the `flex-grow` value.
     */
    flex?: number | string;
    /**
     * Config object of a footer. May contain a `dock`, `html` and a `cls` property. A footer is not a widget,
     * but rather plain HTML that follows the last element of the panel's body and [strips](https://bryntum.com/products/calendar/docs/api/Core/widget/Panel#config-strips).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-footer)
     */
    footer?: {
        dock?: 'top' | 'right' | 'bottom' | 'left' | 'start' | 'end';
        html?: string;
        cls?: string;
    } | string;
    /**
     * Set to `true` to force the time columns to fit to the available space (horizontal or vertical depends on mode).
     * Note that setting [suppressFit](https://bryntum.com/products/calendar/docs/api/Scheduler/view/TimelineBase#config-suppressFit) to `true`, will disable `forceFit` functionality. Zooming
     * cannot be used when `forceFit` is set.
     */
    forceFit?: Boolean;
    /**
     * An object which names formula prefixes which will be applied to all columns configured with
     * `formula : true`.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-formulaProviders)
     */
    formulaProviders?: Record<string, FormulaProviderConfig>;
    /**
     * Refresh entire row when a record changes (`true`) or, if possible, only the cells affected (`false`).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-fullRowRefresh)
     */
    fullRowRefresh?: Boolean;
    /**
     * A method allowing you to define date boundaries that will constrain resize, create and drag drop
     * operations. The method will be called with the Resource record, and the Event record.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-getDateConstraints)
     * @param {Scheduler.model.ResourceModel} resourceRecord The resource record
     * @param {Scheduler.model.EventModel} eventRecord The event record
     * @returns {DateConstraint} Constraining `Object` containing `start` and `end` constraints. Omitting either will mean that end is not constrained. So you can prevent a resize or move from moving *before* a certain time while not constraining the end date.
     */
    getDateConstraints?: (resourceRecord?: ResourceModel, eventRecord?: EventModel) => DateConstraint;
    /**
     * Scheduler overrides Grids default implementation of [getRowHeight](https://bryntum.com/products/calendar/docs/api/Grid/view/GridBase#config-getRowHeight) to
     * pre-calculate row heights based on events in the rows.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-getRowHeight)
     * @param {Scheduler.model.ResourceModel} getRowHeight.record Resource record to determine row height for
     * @returns {number} Desired row height
     */
    getRowHeight?: (getRowHeight: {
        record: ResourceModel;
    }) => number;
    /**
     * A config [object](https://bryntum.com/products/calendar/docs/api/Core/widget/Panel#typedef-PanelHeader) for the panel's header or a string in place of a `title`.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-header)
     */
    header?: string | Boolean | PanelHeader;
    /**
     * Widget's height, used to set element style.height. Either specify a valid height string or a number, which
     * will get 'px' appended. We recommend using CSS as the primary way to control height, but in some cases
     * this config is convenient.
     */
    height?: string | number;
    /**
     * Configure with true to make widget initially hidden.
     */
    hidden?: Boolean;
    /**
     * Set to `true` to hide the footer elements
     */
    hideFooters?: Boolean;
    /**
     * Set to `true` to hide the column header elements
     */
    hideHeaders?: Boolean;
    /**
     * Set to `true` to hide the Grid's horizontal scrollbar(s)
     */
    hideHorizontalScrollbar?: Boolean;
    /**
     * By default the row hover effect is not visible in the Scheduler part of the grid.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-hideRowHover)
     */
    hideRowHover?: Boolean;
    /**
     * Configure as `true`, or set property to `true` to highlight dependent events as well when selecting an event.
     */
    highlightPredecessors?: Boolean;
    /**
     * Configure as `true`, or set property to `true` to highlight dependent events as well when selecting an event.
     */
    highlightSuccessors?: Boolean;
    /**
     * A CSS class to add to hovered row elements
     */
    hoverCls?: string;
    /**
     * Widget id, if not specified one will be generated. Also used for lookups through Widget.getById
     */
    id?: string;
    /**
     * Set to `true` to ignore reacting to DOM events (mouseover/mouseout etc) while scrolling. Useful if you
     * want to maximize scroll performance.
     */
    ignoreDomEventsWhileScrolling?: Boolean;
    /**
     * Determines if the widgets read-only state should be controlled by its parent.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-ignoreParentReadOnly)
     */
    ignoreParentReadOnly?: Boolean;
    /**
     * Set to `true` to automatically adjust the panel timespan during scrolling in the time dimension,
     * when the scroller comes close to the start/end edges.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-infiniteScroll)
     */
    infiniteScroll?: Boolean;
    /**
     * Element (or element id) to insert this widget before. If provided, [appendTo](https://bryntum.com/products/calendar/docs/api/Core/widget/Widget#config-appendTo) config is ignored.
     */
    insertBefore?: HTMLElement | string;
    /**
     * Element (or element id) to append this widget element to, as a first child. If provided, [appendTo](https://bryntum.com/products/calendar/docs/api/Core/widget/Widget#config-appendTo) config is ignored.
     */
    insertFirst?: HTMLElement | string;
    /**
     * A template method (empty by default) allowing you to control if an event can be selected or not.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-isEventSelectable)
     * @param {Scheduler.model.EventModel} event The event record
     * @returns {boolean} true if event can be selected, otherwise false
     */
    isEventSelectable?: (event: EventModel) => Boolean;
    /**
     * See [Keyboard shortcuts](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#keyboard-shortcuts) for details
     */
    keyMap?: Record<string, KeyMapConfig>;
    /**
     * The listener set for this object.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-listeners)
     */
    listeners?: SchedulerListeners;
    /**
     * *This property is for use when the Scheduler is used as a Calendar view - a `mode`.*
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-listRangeMenu)
     */
    listRangeMenu?: MenuConfig;
    /**
     * A [Mask](https://bryntum.com/products/calendar/docs/api/Core/widget/Mask) config object, or a message to be shown when a store is performing a remote
     * operation, or Crud Manager is loading data from the sever. Set to `null` to disable default load mask.
     */
    loadMask?: string | MaskConfig | null;
    /**
     * A [Mask](https://bryntum.com/products/calendar/docs/api/Core/widget/Mask) config object to adjust the [maskDefaults](#Core/widget/Widget#config-maskDefaults)
     * when data is loading. The message and optional configuration from the
     * [loadMask](https://bryntum.com/products/calendar/docs/api/Core/mixin/LoadMaskable#config-loadMask) config take priority over these options, just as they do
     * for `maskDefaults`, respectively.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-loadMaskDefaults)
     */
    loadMaskDefaults?: MaskConfig;
    /**
     * A [Mask](https://bryntum.com/products/calendar/docs/api/Core/widget/Mask) config object to adjust the [maskDefaults](#Core/widget/Widget#config-maskDefaults)
     * when an error occurs loading data.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-loadMaskError)
     */
    loadMaskError?: MaskConfig | Mask | Boolean;
    /**
     * Set to `false` to disable localization of this object.
     */
    localizable?: Boolean;
    /**
     * Time in ms until a longpress is triggered
     */
    longPressTime?: number;
    /**
     * This flag controls whether Scheduler should preserve its selection of events when loading a new dataset
     * (if selected event ids are included in the newly loaded dataset).
     */
    maintainSelectionOnDatasetChange?: Boolean;
    /**
     * When `true`, events are sized and positioned based on rowHeight, resourceMargin and barMargin settings.
     * Set this to `false` if you want to control height and vertical position using CSS instead.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-managedEventSizing)
     */
    managedEventSizing?: Boolean;
    /**
     * Widget's margin. This may be configured as a single number or a `TRBL` format string.
     * numeric-only values are interpreted as pixels.
     */
    margin?: number | string;
    /**
     * Grids change the `maskDefaults` to cover only their `body` element.
     */
    maskDefaults?: MaskConfig;
    /**
     * Set to `true` to apply the default mask to the widget. Alternatively, this can be the mask message or a
     * [Mask](https://bryntum.com/products/calendar/docs/api/Core/widget/Mask) config object.
     */
    masked?: Boolean | string | MaskConfig;
    /**
     * Get/set end date limit of the timeline. Actions such as timeline scrolling, all types of timeline zooms and shifts will respect this limit.
     */
    maxDate?: Date | string;
    /**
     * The element's maxHeight. Can be either a String or a Number (which will have 'px' appended). Note that
     * like [height](https://bryntum.com/products/calendar/docs/api/Core/widget/Widget#config-height), *reading* the value will return the numeric value in pixels.
     */
    maxHeight?: string | number;
    /**
     * The maximum time axis unit to display non-working ranges for ('hour' or 'day' etc).
     * When zooming to a view with a larger unit, no non-working time elements will be rendered.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-maxTimeAxisUnit)
     */
    maxTimeAxisUnit?: DurationUnit;
    /**
     * The elements maxWidth. Can be either a String or a Number (which will have 'px' appended). Note that
     * like [width](https://bryntum.com/products/calendar/docs/api/Core/widget/Widget#config-width), *reading* the value will return the numeric value in pixels.
     */
    maxWidth?: string | number;
    /**
     * The maximum zoom level to which [zoomIn](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/TimelineZoomable#function-zoomIn) will work. Defaults to the number of
     * [ViewPresets](https://bryntum.com/products/calendar/docs/api/Scheduler/preset/ViewPreset) available, see [presets](#Scheduler/view/mixin/TimelineViewPresets#property-presets)
     * for information. Unless you have modified the collection of available presets, the max zoom level is
     * milliseconds.
     */
    maxZoomLevel?: number;
    /**
     * How to align milestones in relation to their startDate. Only applies when using a `milestoneLayoutMode`
     * other than `default`. Valid values are:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-milestoneAlign)
     */
    milestoneAlign?: 'start' | 'center' | 'end';
    /**
     * Factor representing the average char width in pixels used to determine milestone width when configured
     * with `milestoneLayoutMode: 'estimate'`.
     */
    milestoneCharWidth?: number;
    /**
     * How to handle milestones during event layout. How the milestones are displayed when part of the layout
     * are controlled using [milestoneTextPosition](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/SchedulerEventRendering#config-milestoneTextPosition).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-milestoneLayoutMode)
     */
    milestoneLayoutMode?: 'default' | 'estimate' | 'data' | 'measure';
    /**
     * Position of the milestone text:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-milestoneTextPosition)
     */
    milestoneTextPosition?: 'inside' | 'outside' | 'always-outside';
    /**
     * Get/set start date limit of the timeline. Actions such as timeline scrolling, all types of timeline zooms and shifts will respect this limit.
     */
    minDate?: Date | string;
    /**
     * Grid's `min-height`. Defaults to `10em` to be sure that the Grid always has a height wherever it is
     * inserted.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-minHeight)
     */
    minHeight?: string | number;
    /**
     * The elements minWidth. Can be either a String or a Number (which will have 'px' appended). Note that
     * like [width](https://bryntum.com/products/calendar/docs/api/Core/widget/Widget#config-width), *reading* the value will return the numeric value in pixels.
     */
    minWidth?: string | number;
    /**
     * The minimum zoom level to which [zoomOut](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/TimelineZoomable#function-zoomOut) will work. Defaults to 0 (year ticks)
     */
    minZoomLevel?: number;
    /**
     * Scheduler mode. Supported values: horizontal, vertical
     */
    mode?: 'horizontal' | 'vertical';
    /**
     * Grid monitors window resize by default.
     */
    monitorResize?: Boolean;
    /**
     * Configure as `true` to allow `CTRL+click` to select multiple events in the scheduler.
     */
    multiEventSelect?: Boolean;
    /**
     * When an event bar has a width less than this value, it gets the CSS class `b-sch-event-narrow`
     * added. You may apply custom CSS rules using this class.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-narrowEventWidth)
     */
    narrowEventWidth?: number;
    /**
     * Override this method to provide a custom sort function to sort any overlapping events. This only applies
     * to the horizontal mode, where the order the events are sorted in determines their vertical placement
     * within a resource.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-overlappingEventSorter)
     * @param {Scheduler.model.EventModel} a First event
     * @param {Scheduler.model.EventModel} b Second event
     * @returns {number} Return -1 to display `a` above `b`, 1 for `b` above `a`
     */
    overlappingEventSorter?: (a: EventModel, b: EventModel) => number;
    /**
     * The owning Widget of this Widget. If this Widget is directly contained (that is, it is one of the
     * [items](https://bryntum.com/products/calendar/docs/api/Core/widget/Container#property-items) of a Container), this config will be ignored. In this case
     * the owner is <strong>always</strong> the encapsulating Container.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-owner)
     */
    owner?: Widget;
    /**
     * Partners this Timeline panel with another Timeline in order to sync their region sizes (sub-grids like locked, normal will get the same width),
     * start and end dates, view preset, zoom level and scrolling position. All these values will be synced with the timeline defined as the `partner`.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-partner)
     */
    partner?: TimelineBase;
    /**
     * Set to `true` to include `startDate` and `endDate` params indicating the currently viewed date range in
     * `EventStore` load requests (when loading using `AjaxStore` or `CrudManager` functionality).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-passStartEndParameters)
     */
    passStartEndParameters?: Boolean;
    /**
     * Specify plugins (an array of classes) in config
     */
    plugins?: Function[];
    /**
     * Maximum number of resources for which height is pre-calculated. If you have many events per
     * resource you might want to lower this number to gain some initial rendering performance.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-preCalculateHeightLimit)
     */
    preCalculateHeightLimit?: number;
    /**
     * True to preserve focused cell after loading new data
     */
    preserveFocusOnDatasetChange?: Boolean;
    /**
     * Preserve the grid's vertical scroll position when changesets are applied, as in the case of remote
     * changes, or when stores are configured with [syncDataOnLoad](https://bryntum.com/products/calendar/docs/api/Core/data/Store#config-syncDataOnLoad).
     */
    preserveScroll?: PreserveScrollOptions | Boolean;
    /**
     * Specify `true` to preserve vertical scroll position after store actions that trigger a `refresh` event,
     * such as loading new data and filtering.
     */
    preserveScrollOnDatasetChange?: Boolean;
    /**
     * An array of [ViewPreset](https://bryntum.com/products/calendar/docs/api/Scheduler/preset/ViewPreset) config objects
     * which describes the available timeline layouts for this scheduler.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-presets)
     */
    presets?: ViewPresetConfig[];
    /**
     * Prevent tooltip from being displayed on touch devices. Useful for example for buttons that display a
     * menu on click etc, since the tooltip would be displayed at the same time.
     */
    preventTooltipOnTouch?: Boolean;
    /**
     * A [ProjectModel](https://bryntum.com/products/calendar/docs/api/Scheduler/model/ProjectModel) instance or a config object. The project holds all Scheduler data.
     * Can be omitted in favor of individual store configs or [crudManager](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/SchedulerStores#config-crudManager) config.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-project)
     */
    project?: ProjectModel | ProjectModelConfig | object;
    /**
     * Range used to set the length of the time axis when used as a component of a Calendar. Suitable units are
     * `'month'`, `'week'` and `'day'`.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-range)
     */
    range?: string | DurationConfig;
    /**
     * Configure as `true` to make the scheduler read-only, by disabling any UIs for modifying data.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-readOnly)
     */
    readOnly?: Boolean;
    /**
     * The confirmation dialog shown when a recurring event is edited.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-recurrenceConfirmationPopup)
     */
    recurrenceConfirmationPopup?: RecurrenceConfirmationPopupConfig | RecurrenceConfirmationPopup;
    relayStoreEvents?: Boolean;
    /**
     * Configure this property to allow the widget/component to be resized. Pressing <kbd>Shift</kbd> while resizing will
     * constrain the aspect ratio.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-resizable)
     */
    resizable?: Boolean | {
        minWidth?: number;
        maxWidth?: number;
        minHeight?: number;
        maxHeight?: number;
        handles?: object;
    };
    /**
     * Set to `false` to only measure cell contents when double-clicking the edge between column headers.
     */
    resizeToFitIncludesHeader?: Boolean;
    /**
     * A config object used to configure the resource columns in vertical mode.
     * See [ResourceHeader](https://bryntum.com/products/calendar/docs/api/Scheduler/view/ResourceHeader) for more details on available properties.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-resourceColumns)
     */
    resourceColumns?: ResourceHeaderConfig;
    /**
     * Resource image extension, used when creating image path from resource name.
     */
    resourceImageExtension?: string;
    /**
     * Path to load resource images from. Used by the resource header in vertical mode and the
     * [ResourceInfoColumn](https://bryntum.com/products/calendar/docs/api/Scheduler/column/ResourceInfoColumn) in horizontal mode. Set this to display miniature
     * images for each resource using their `image` or `imageUrl` fields.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-resourceImagePath)
     */
    resourceImagePath?: string;
    /**
     * Control how much space to leave between the first event/last event and the resources edge (top/bottom
     * margin within the resource row in horizontal mode, left/right margin within the resource column in
     * vertical mode), in px. Defaults to the value of [barMargin](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-barMargin).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-resourceMargin)
     */
    resourceMargin?: number | ResourceMarginConfig;
    /**
     * Inline resources, will be loaded into an internally created ResourceStore.
     */
    resources?: ResourceModel[] | ResourceModelConfig[];
    /**
     * The [ResourceStore](https://bryntum.com/products/calendar/docs/api/Scheduler/data/ResourceStore) holding the resources to be rendered into the scheduler (required).
     */
    resourceStore?: ResourceStore | ResourceStoreConfig;
    /**
     * An empty function by default, but provided so that you can override it. This function is called each time
     * a resource time range is rendered into the schedule. It's called with `resourceTimeRangeRecord`, `resourceRecord`,
     * and `renderData` params.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-resourceTimeRangeRenderer)
     * @param {object} detail An object containing the information needed to render a ResourceTimeRangeModel.
     * @param {Scheduler.model.ResourceTimeRangeModel} detail.resourceTimeRangeRecord The resource time range record.
     * @param {Scheduler.model.ResourceModel} detail.resourceRecord The resource record.
     * @param {object} detail.renderData An object containing details about the event rendering.
     * @returns {string,DomConfig,DomConfig[]} A simple string, or a DomConfig object describing the markup
     */
    resourceTimeRangeRenderer?: (detail: {
        resourceTimeRangeRecord: ResourceTimeRangeModel;
        resourceRecord: ResourceModel;
        renderData: object;
    }) => string | DomConfig | DomConfig[];
    /**
     * Inline resource time ranges, will be loaded into an internally created store if
     * [ResourceTimeRanges](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/ResourceTimeRanges) is enabled.
     */
    resourceTimeRanges?: ResourceTimeRangeModel[] | ResourceTimeRangeModelConfig[];
    /**
     * Resource time ranges store instance or config object for [ResourceTimeRanges](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/ResourceTimeRanges) feature.
     */
    resourceTimeRangeStore?: ResourceTimeRangeStore | ResourceTimeRangeStoreConfig;
    /**
     * "Break points" for which responsive config to use for columns and css.
     */
    responsiveLevels?: Record<string, number | string>;
    /**
     * Configure as `true` to have the component display a translucent ripple when its
     * [focusElement](https://bryntum.com/products/calendar/docs/api/Core/widget/Widget#property-focusElement), or [element](#Core/widget/Widget#property-element) is tapped <em>if the
     * current theme supports ripples</em>. Out of the box, only the Material theme supports ripples.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-ripple)
     */
    ripple?: Boolean | {
        delegate?: string;
        color?: string;
        radius?: number;
        clip?: string;
    };
    /**
     * If you are rendering this widget to a shadow root inside a web component, set this config to the shadowRoot. If not inside a web component, set it to `document.body`
     */
    rootElement?: ShadowRoot | HTMLElement;
    /**
     * The height in pixels of Scheduler rows.
     */
    rowHeight?: number;
    /**
     * Set to `false` to not show row lines. End result might be overruled by/differ between themes.
     */
    rowLines?: Boolean;
    /**
     * This may be configured as `true` to make the widget's element use the `direction:rtl` style.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-rtl)
     */
    rtl?: Boolean;
    /**
     * Configures whether the grid is scrollable in the `Y` axis. This is used to configure a [Scroller](https://bryntum.com/products/calendar/docs/api/Core/helper/util/Scroller).
     * See the [scrollerClass](https://bryntum.com/products/calendar/docs/api/Grid/view/GridBase#config-scrollerClass) config option.
     */
    scrollable?: Boolean | ScrollerConfig | Scroller;
    /**
     * The class to instantiate to use as the [scrollable](https://bryntum.com/products/calendar/docs/api/Grid/view/GridBase#config-scrollable). Defaults to [Scroller](#Core/helper/util/Scroller).
     */
    scrollerClass?: typeof Scroller;
    /**
     * Specifies the maximum duration for extending the current [TimeSpan](https://bryntum.com/products/calendar/docs/api/Scheduler/model/TimeSpan) to allow for smooth
     * scrolling to an event.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-scrollExtensionThreshold)
     */
    scrollExtensionThreshold?: string | DurationConfig;
    /**
     * Configuration values for the [ScrollManager](https://bryntum.com/products/calendar/docs/api/Core/util/ScrollManager) class on initialization. Returns the
     * [ScrollManager](https://bryntum.com/products/calendar/docs/api/Core/util/ScrollManager) at runtime.
     */
    scrollManager?: ScrollManagerConfig | ScrollManager;
    /**
     * Selection configuration settings, change these properties to control how selection works and what can be
     * selected.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-selectionMode)
     */
    selectionMode?: GridSelectionMode;
    /**
     * Set to `false` to not select the resource of the event when clicking an event bar.
     */
    selectResourceOnEventNavigate?: Boolean;
    /**
     * Set to `false` to not select the row/resource when clicking the empty area in a time axis cell.
     */
    selectResourceOnScheduleClick?: Boolean;
    /**
     * <div class="note">This property is for use when the Scheduler is used as a Calendar view - a `mode`.
     * it will have no effect on a Scheduler which is not inside a Calendar.</div>
     * The time range to move by when [next](https://bryntum.com/products/calendar/docs/api/Scheduler/view/SchedulerBase#function-next) or [previous](#Scheduler/view/SchedulerBase#function-previous) is called.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-shiftIncrement)
     */
    shiftIncrement?: string | DurationConfig;
    /**
     * `false` to not show a tooltip while creating a dependency
     */
    showCreationTooltip?: Boolean;
    /**
     * Configure as `true` to have the grid show a red "changed" tag in cells whose
     * field value has changed and not yet been committed.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-showDirty)
     */
    showDirty?: Boolean | {
        duringEdit?: Boolean;
    };
    /**
     * If set to `true` this will show a color field in the [EventEdit](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventEdit) editor and also a
     * picker in the [EventMenu](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventMenu). Both enables the user to choose a color which will be
     * applied to the event bar's background. See EventModel's
     * [eventColor](https://bryntum.com/products/calendar/docs/api/Scheduler/model/mixin/EventModelMixin#field-eventColor) config.
     * config.
     */
    showEventColorPickers?: Boolean;
    /**
     * Set to `true` to show the lag in the tooltip
     */
    showLagInTooltip?: Boolean;
    /**
     * Set to `false` to hide recurring fields in event editor, even if the
     * [Recurring Events](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/RecurringEvents#config-enableRecurringEvents) is `true`
     * and a recurring event is being edited.
     */
    showRecurringUI?: Boolean;
    /**
     * Set to `true` to show a tooltip when hovering a dependency line
     */
    showTooltip?: Boolean;
    /**
     * Set to `true` to snap to the current time resolution increment while interacting with scheduled events.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-snap)
     */
    snap?: Boolean;
    /**
     * Affects drag drop and resizing of events when [snap](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/TimelineDateMapper#config-snap)
     * is enabled.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-snapRelativeToEventStartDate)
     */
    snapRelativeToEventStartDate?: Boolean;
    /**
     * The start date of the timeline (if not configure with [infiniteScroll](https://bryntum.com/products/calendar/docs/api/Scheduler/view/TimelineBase#config-infiniteScroll)).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-startDate)
     */
    startDate?: Date | string;
    /**
     * The name of the start date parameter that will be passed to in every `eventStore` load request.
     */
    startParamName?: string;
    /**
     * This value can be one of the following:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-stateful)
     */
    stateful?: Boolean | object | string[];
    /**
     * The events that, when fired by this component, should trigger it to save its state by calling
     * [saveState](https://bryntum.com/products/calendar/docs/api/Core/mixin/State#function-saveState).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-statefulEvents)
     */
    statefulEvents?: object | string[];
    /**
     * The key to use when saving this object's state in the [stateProvider](https://bryntum.com/products/calendar/docs/api/Core/mixin/State#config-stateProvider). If this config is
     * not assigned, and [stateful](https://bryntum.com/products/calendar/docs/api/Core/mixin/State#config-stateful) is not set to `false`, the [id](#Core/widget/Widget#config-id)
     * (if explicitly specified) will be used as the `stateId`.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-stateId)
     */
    stateId?: string;
    /**
     * The `StateProvider` to use to save and restore this object's [state](https://bryntum.com/products/calendar/docs/api/Core/mixin/State#property-state). By default, `state`
     * will be saved using the [default state provider](https://bryntum.com/products/calendar/docs/api/Core/state/StateProvider#property-instance-static).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-stateProvider)
     */
    stateProvider?: StateProvider;
    /**
     * The properties of this settings object controls how grid is restored from state data.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-stateSettings)
     */
    stateSettings?: {
        restoreUnconfiguredColumns?: Boolean;
    };
    /**
     * The time unit to move by when [next](https://bryntum.com/products/calendar/docs/api/Scheduler/view/SchedulerBase#function-next) or [previous](#Scheduler/view/SchedulerBase#function-previous) is called.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-stepUnit)
     * @deprecated 6.1.6 Use [shiftIncrement](https://bryntum.com/products/calendar/docs/api/Scheduler/view/SchedulerBase#config-shiftIncrement) instead.
     */
    stepUnit?: DurationUnit;
    /**
     * When set, the text in the major time axis header sticks in the scrolling viewport as long as possible.
     */
    stickyHeaders?: Boolean;
    /**
     * Store that holds records to display in the grid, or a store config object. If the configuration contains
     * a `readUrl`, an `AjaxStore` will be created.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-store)
     */
    store?: Store | StoreConfig | AjaxStore | AjaxStoreConfig;
    /**
     * An object containing widgets keyed by name. By default (when no `type` is given), strips are
     * [toolbars](https://bryntum.com/products/calendar/docs/api/Core/widget/Toolbar). If you want to pass an array, you can use
     * the toolbar's [items](https://bryntum.com/products/calendar/docs/api/Core/widget/Container#config-items).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-strips)
     */
    strips?: Record<string, ContainerItemConfig>;
    /**
     * An object containing sub grid configuration objects keyed by a `region` property.
     * By default, grid has a 'locked' region (if configured with locked columns) and a 'normal' region.
     * The 'normal' region defaults to use `flex: 1`.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-subGridConfigs)
     */
    subGridConfigs?: Record<string, SubGridConfig>;
    /**
     * Set to `true` to prevent auto calculating of a minimal [tickSize](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/TimelineEventRendering#property-tickSize)
     * to always fit the content to the screen size. Setting this property on `true` will disable [forceFit](https://bryntum.com/products/calendar/docs/api/Scheduler/view/TimelineBase#config-forceFit) behaviour.
     */
    suppressFit?: Boolean;
    /**
     * A [Mask](https://bryntum.com/products/calendar/docs/api/Core/widget/Mask) config object, or a message to be shown when Crud Manager
     * is persisting changes on the server. Set to `null` to disable default sync mask.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-syncMask)
     */
    syncMask?: string | MaskConfig | null;
    /**
     * A configuration for the [tab](https://bryntum.com/products/calendar/docs/api/Core/widget/Tab) created for this widget when it is placed in a
     * [TabPanel](https://bryntum.com/products/calendar/docs/api/Core/widget/TabPanel). For example, this config can be used to control the icon of the `tab` for
     * this widget:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-tab)
     */
    tab?: Boolean | TabConfig;
    /**
     * When this container is used as a tab in a TabPanel, these items are added to the
     * [TabBar](https://bryntum.com/products/calendar/docs/api/Core/widget/TabBar) when this container is the active tab.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-tabBarItems)
     */
    tabBarItems?: ToolbarItems[] | Widget[];
    /**
     * A Config object representing the configuration of a [Toolbar](https://bryntum.com/products/calendar/docs/api/Core/widget/Toolbar),
     * or array of config objects representing the child items of a Toolbar.
     * This creates a toolbar docked to the top of the panel immediately below the header.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-tbar)
     */
    tbar?: (ContainerItemConfig | string)[] | ToolbarConfig | PagingToolbarConfig | null;
    /**
     * CSS class used for terminals
     */
    terminalCls?: string;
    /**
     * Delay in ms before hiding the terminals when the mouse leaves an event bar or terminal.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-terminalHideDelay)
     */
    terminalHideDelay?: number;
    /**
     * Terminal offset from their initial position, in px. Positive values move terminals further away from the
     * event bar, negative values inside the event bar.
     */
    terminalOffset?: number;
    /**
     * Delay in ms before showing the terminals when hovering over an event bar.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-terminalShowDelay)
     */
    terminalShowDelay?: number;
    /**
     * Where (on event bar edges) to display terminals. The sides are `'start'`, `'top'`,
     * `'end'` and `'bottom'`
     */
    terminalSides?: string[];
    /**
     * Terminal diameter in px, overrides the default CSS value for it (which might depend on theme).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-terminalSize)
     */
    terminalSize?: number | string;
    /**
     * The width/height (depending on vertical / horizontal mode) of all the time columns.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-tickSize)
     */
    tickSize?: number;
    /**
     * A [TimeAxis](https://bryntum.com/products/calendar/docs/api/Scheduler/data/TimeAxis) config object or instance, used to create a backing data store of
     * 'ticks' providing the input date data for the time axis of timeline panel. Created automatically if none
     * supplied.
     */
    timeAxis?: TimeAxisConfig | TimeAxis;
    /**
     * Inline time ranges, will be loaded into an internally created store if [TimeRanges](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/TimeRanges)
     * is enabled.
     */
    timeRanges?: TimeSpan[] | TimeSpanConfig[];
    /**
     * The time ranges store instance for [TimeRanges](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/TimeRanges) feature.
     */
    timeRangeStore?: Store | StoreConfig;
    /**
     * Gets/sets the current time resolution object, which contains a unit identifier and an increment count
     * `{ unit, increment }`. This value means minimal task duration you can create using UI.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-timeResolution)
     */
    timeResolution?: object | number;
    /**
     * Set to a time zone or a UTC offset. This will set the projects
     * [timeZone](https://bryntum.com/products/calendar/docs/api/Scheduler/model/ProjectModel#config-timeZone) config accordingly. As this config is only a referer,
     * please see project's config [documentation](https://bryntum.com/products/calendar/docs/api/Scheduler/model/ProjectModel#config-timeZone) for more
     * information.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-timeZone)
     */
    timeZone?: string | number;
    /**
     * A title to display in the header or owning TabPanel. Causes creation and docking of a header
     * to the top if no header is configured.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-title)
     */
    title?: string;
    /**
     * The [tools](https://bryntum.com/products/calendar/docs/api/Core/widget/Tool) to add either before or after the `title` in the Panel header. Each
     * property name is the reference by which an instantiated tool may be retrieved from the live
     * `[tools](https://bryntum.com/products/calendar/docs/api/Core/widget/mixin/Toolable#property-tools)` property.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-tools)
     */
    tools?: Record<string, ToolConfig> | null;
    /**
     * A template function allowing you to configure the contents of the tooltip shown when hovering a
     * dependency line. You can return either an HTML string or a [DomConfig](https://bryntum.com/products/calendar/docs/api/Core/helper/DomHelper#typedef-DomConfig) object.
     * @param {Scheduler.model.DependencyBaseModel} dependency The dependency record
     * @returns {string,DomConfig}
     */
    tooltipTemplate?: (dependency: DependencyBaseModel) => string | DomConfig;
    /**
     * Animation transition duration in milliseconds.
     */
    transitionDuration?: number;
    /**
     * Configure as `true` to trigger `selectionChange` when removing a selected event/assignment.
     */
    triggerSelectionChangeOnRemove?: Boolean;
    type?: 'scheduler';
    /**
     * Custom CSS class name suffixes to apply to the elements rendered by this widget. This may be specified
     * as a space separated string, an array of strings, or as an object in which property names with truthy
     * values are used as the class names.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-ui)
     */
    ui?: 'plain' | 'toolbar' | string | object;
    /**
     * By default, scrolling the schedule will update the [timelineContext](https://bryntum.com/products/calendar/docs/api/Scheduler/view/SchedulerBase#property-timelineContext) to reflect the new
     * currently hovered context. When displaying a large number of events on screen at the same time, this will
     * have a slight impact on scrolling performance. In such scenarios, opt out of this behavior by setting
     * this config to `false`.
     */
    updateTimelineContextOnScroll?: Boolean;
    /**
     * By default, the day of week and week of month of the event's start date are used to create
     * helpful contextual recurrence types when editing a non-recurring event record and choosing
     * an initial recurrence type.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-useContextualRecurrenceRules)
     */
    useContextualRecurrenceRules?: Boolean;
    /**
     * By default, scheduler fade events in on load. Specify `false` to prevent this animation or specify one
     * of the available animation types to use it (`true` equals `'fade-in'`):
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-useInitialAnimation)
     */
    useInitialAnimation?: Boolean | 'fade-in' | 'slide-from-left' | 'slide-from-top' | string;
    /**
     * The time axis column config for vertical [mode](https://bryntum.com/products/calendar/docs/api/Scheduler/view/SchedulerBase#config-mode).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-verticalTimeAxisColumn)
     */
    verticalTimeAxisColumn?: VerticalTimeAxisColumnConfig;
    /**
     * A string key used to lookup a predefined [ViewPreset](https://bryntum.com/products/calendar/docs/api/Scheduler/preset/ViewPreset) (e.g. 'weekAndDay', 'hourAndDay'),
     * managed by [PresetManager](https://bryntum.com/products/calendar/docs/api/Scheduler/preset/PresetManager). See [PresetManager](#Scheduler/preset/PresetManager) for more information.
     * Or a config object for a viewPreset.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-viewPreset)
     */
    viewPreset?: string | ViewPresetConfig;
    /**
     * A date to bring into view initially on the scrollable timeline.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-visibleDate)
     */
    visibleDate?: Date | VisibleDate;
    /**
     * Integer number indicating the size of timespan during zooming. When zooming, the timespan is adjusted to make
     * the scrolling area `visibleZoomFactor` times wider than the timeline area itself. Used in
     * [zoomToSpan](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/TimelineZoomable#function-zoomToSpan) and [zoomToLevel](#Scheduler/view/mixin/TimelineZoomable#function-zoomToLevel) functions.
     */
    visibleZoomFactor?: number;
    /**
     * A valid JS day index between 0-6 (0: Sunday, 1: Monday etc.) to be considered the start day of the week.
     * When omitted, the week start day is retrieved from the active locale class.
     */
    weekStartDay?: number;
    /**
     * A widgets weight determines its position among siblings when added to a [Container](https://bryntum.com/products/calendar/docs/api/Core/widget/Container).
     * Higher weights go further down.
     */
    weight?: number;
    /**
     * Widget's width, used to set element style.width. Either specify a valid width string or a number, which
     * will get 'px' appended. We recommend using CSS as the primary way to control width, but in some cases
     * this config is convenient.
     */
    width?: string | number;
    /**
     * An object with format `{ fromDay, toDay, fromHour, toHour }` that describes the working days and hours.
     * This object will be used to populate TimeAxis [include](https://bryntum.com/products/calendar/docs/api/Scheduler/data/TimeAxis#config-include) property.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/view/Scheduler#config-workingTime)
     */
    workingTime?: {
        fromDay?: number;
        toDay?: number;
        fromHour?: number;
        toHour?: number;
    };
    /**
     * Whether the originally rendered timespan should be preserved while zooming. By default, it is set to `false`,
     * meaning the timeline panel will adjust the currently rendered timespan to limit the amount of HTML content to
     * render. When setting this option to `true`, be careful not to allow to zoom a big timespan in seconds
     * resolution for example. That will cause <strong>a lot</strong> of HTML content to be rendered and affect performance. You
     * can use [minZoomLevel](https://bryntum.com/products/calendar/docs/api/Scheduler/view/mixin/TimelineZoomable#config-minZoomLevel) and [maxZoomLevel](#Scheduler/view/mixin/TimelineZoomable#config-maxZoomLevel) config options for that.
     */
    zoomKeepsOriginalTimespan?: Boolean;
    /**
     * If true, you can zoom in and out on the time axis using CTRL-key + mouse wheel.
     */
    zoomOnMouseWheel?: Boolean;
    /**
     * True to zoom to time span when double-clicking a time axis cell.
     */
    zoomOnTimeAxisDoubleClick?: Boolean;
    /**
     * Allows using `[Ctrl/CMD + C]`, `[Ctrl/CMD + X]` and `[Ctrl/CMD + V]` to cut, copy and paste cell or cell ranges. Also
     * makes cut, copy and paste actions available via the cell context menu.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/CellCopyPaste)
     */
    cellCopyPasteFeature?: object | Boolean | string | CellCopyPaste | CellCopyPasteConfig;
    /**
     * Adding this feature to the grid and other Bryntum products which are based on the Grid (i.e. Scheduler, SchedulerPro, and Gantt)
     * enables cell editing. Any subclass of [Field](https://bryntum.com/products/calendar/docs/api/Core/widget/Field) can be used
     * as editor for the [Column](https://bryntum.com/products/calendar/docs/api/Grid/column/Column). The most popular are:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/CellEdit)
     */
    cellEditFeature?: object | Boolean | string | CellEdit | CellEditConfig;
    /**
     * Right click to display context menu for cells.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/CellMenu)
     */
    cellMenuFeature?: object | Boolean | string | CellMenu | CellMenuConfig;
    /**
     * Displays a tooltip when hovering cells.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/CellTooltip)
     */
    cellTooltipFeature?: object | Boolean | string | CellTooltip | CellTooltipConfig;
    /**
     * Enables the [autoWidth](https://bryntum.com/products/calendar/docs/api/Grid/column/Column#config-autoWidth) config for a grid's columns.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/ColumnAutoWidth)
     */
    columnAutoWidthFeature?: object | Boolean | string | ColumnAutoWidth | ColumnAutoWidthConfig;
    /**
     * Displays a toolbar while dragging column headers. Drop on a button in the toolbar to activate a certain function,
     * for example to group by that column. This feature simplifies certain operations on touch devices.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/ColumnDragToolbar)
     */
    columnDragToolbarFeature?: object | Boolean | string | ColumnDragToolbar | ColumnDragToolbarConfig;
    /**
     * Displays column lines for ticks, with a different styling for major ticks (by default they are darker). If this
     * feature is disabled, no lines are shown. If it's enabled, line are shown for the tick level which is set in current
     * ViewPreset. Please see [columnLinesFor](https://bryntum.com/products/calendar/docs/api/Scheduler/preset/ViewPreset#field-columnLinesFor) config for details.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/ColumnLines)
     */
    columnLinesFeature?: object | Boolean | string | ColumnLines | ColumnLinesConfig;
    /**
     * Displays a column picker (to show/hide columns) in the header context menu. Columns can be displayed in sub menus
     * by region or tag. Grouped headers are displayed as menu hierarchies.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/ColumnPicker)
     */
    columnPickerFeature?: object | Boolean | string | ColumnPicker | ColumnPickerConfig;
    /**
     * Allows user to rename columns by either right-clicking column header or using keyboard shortcuts when column header
     * is focused.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/ColumnRename)
     */
    columnRenameFeature?: object | Boolean | string | ColumnRename | ColumnRenameConfig;
    /**
     * Allows user to reorder columns by dragging headers. To get notified about column reorder listen to `change` event
     * on [columns](https://bryntum.com/products/calendar/docs/api/Grid/data/ColumnStore) store.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/ColumnReorder)
     */
    columnReorderFeature?: object | Boolean | string | ColumnReorder | ColumnReorderConfig;
    /**
     * Enables user to resize columns by dragging a handle on the right hand side of the header. To get notified about column
     * resize listen to `change` event on [columns](https://bryntum.com/products/calendar/docs/api/Grid/data/ColumnStore) store.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/ColumnResize)
     */
    columnResizeFeature?: object | Boolean | string | ColumnResize | ColumnResizeConfig;
    /**
     * Feature that draws dependencies between events. Uses a [DependencyStore](https://bryntum.com/products/calendar/docs/api/Scheduler/data/DependencyStore) to determine which
     * dependencies to draw, if none is defined one will be created automatically. Dependencies can also be specified as
     * `scheduler.dependencies`, see example below:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/Dependencies)
     */
    dependenciesFeature?: object | Boolean | string | Dependencies | DependenciesConfig;
    /**
     * Feature that displays a popup containing fields for editing a dependency. Requires the
     * [Dependencies](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/Dependencies) feature to be enabled. Double-click a dependency line in the demo below to
     * show the editor.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/DependencyEdit)
     */
    dependencyEditFeature?: object | Boolean | string | DependencyEdit | DependencyEditConfig;
    /**
     * Displays a context menu when right-clicking dependency lines. Items are populated by other features and/or
     * application code.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/DependencyMenu)
     */
    dependencyMenuFeature?: object | Boolean | string | DependencyMenu | DependencyMenuConfig;
    /**
     * Allow using [Ctrl/CMD + C/X] and [Ctrl/CMD + V] to copy/cut and paste events.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventCopyPaste)
     */
    eventCopyPasteFeature?: object | Boolean | string | SchedulerEventCopyPaste | SchedulerEventCopyPasteConfig;
    /**
     * Allows user to drag and drop events within the scheduler, to change `startDate` or resource assignment.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventDrag)
     */
    eventDragFeature?: object | Boolean | string | EventDrag | EventDragConfig;
    /**
     * Feature that allows the user to create new events by dragging in empty parts of the scheduler rows.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventDragCreate)
     */
    eventDragCreateFeature?: object | Boolean | string | EventDragCreate | EventDragCreateConfig;
    /**
     * Enables users to click and drag to select events (or assignments in multi assignment mode) inside the Scheduler's
     * timeline. Press CTRL/CMD-key to extend an existing selection.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventDragSelect)
     */
    eventDragSelectFeature?: object | Boolean | string | EventDragSelect | EventDragSelectConfig;
    /**
     * Feature that displays a popup containing widgets for editing event data.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventEdit)
     */
    eventEditFeature?: object | Boolean | string | SchedulerEventEdit | SchedulerEventEditConfig;
    /**
     * Adds event filter menu items to the timeline header context menu.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventFilter)
     */
    eventFilterFeature?: object | Boolean | string | EventFilter | EventFilterConfig;
    /**
     * Displays a context menu for events. Items are populated by other features and/or application code.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventMenu)
     */
    eventMenuFeature?: object | Boolean | string | SchedulerEventMenu | SchedulerEventMenuConfig;
    /**
     * Feature that allows rendering non-working time ranges into event bars (weekends for Scheduler, effective calendar for
     * SchedulerPro):
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventNonWorkingTime)
     */
    eventNonWorkingTimeFeature?: object | Boolean | string | EventNonWorkingTime | EventNonWorkingTimeConfig;
    /**
     * Feature that allows resizing an event by dragging its end.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventResize)
     */
    eventResizeFeature?: object | Boolean | string | EventResize | EventResizeConfig;
    /**
     * Displays a tooltip when hovering events. The template used to render the tooltip can be customized, see [template](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventTooltip#config-template).
     * Config options are also applied to the tooltip shown, see [Tooltip](https://bryntum.com/products/calendar/docs/api/Core/widget/Tooltip) for available options.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/EventTooltip)
     */
    eventTooltipFeature?: object | Boolean | string | SchedulerEventTooltip | SchedulerEventTooltipConfig;
    /**
     * <strong>NOTE</strong>: This class requires a 3rd party library to operate.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/experimental/ExcelExporter)
     */
    excelExporterFeature?: object | Boolean | string | SchedulerExcelExporter | SchedulerExcelExporterConfig;
    /**
     * An experimental feature that lets users drop files on a Widget. The widget fires an event when a file is dropped onto it.
     * In the event, you get access to the raw files as strings, that were parsed by calling `readAsBinaryString`.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/experimental/FileDrop)
     */
    fileDropFeature?: object | Boolean | string | FileDrop | FileDropConfig;
    /**
     * This feature adds a fill handle to a Grid range selection, which when dragged, fills the cells being dragged over
     * with values based on the values in the original selected range. This is similar to functionality normally seen in
     * various spreadsheet applications.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/FillHandle)
     */
    fillHandleFeature?: object | Boolean | string | FillHandle | FillHandleConfig;
    /**
     * Feature that allows filtering of the grid by settings filters on columns. The actual filtering is done by the store.
     * For info on programmatically handling filters, see [StoreFilter](https://bryntum.com/products/calendar/docs/api/Core/data/mixin/StoreFilter).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/Filter)
     */
    filterFeature?: object | Boolean | string | Filter | FilterConfig;
    /**
     * Feature that allows filtering of the grid by entering filters on column headers.
     * The actual filtering is done by the store.
     * For info on programmatically handling filters, see [StoreFilter](https://bryntum.com/products/calendar/docs/api/Core/data/mixin/StoreFilter).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/FilterBar)
     */
    filterBarFeature?: object | Boolean | string | FilterBar | FilterBarConfig;
    /**
     * Enables rendering and handling of row groups. The actual grouping is done in the store, but triggered by `[shift]` +
     * clicking headers or by using two finger tap (one on header, one anywhere on grid). Use `[shift]` + `[alt]` + click to
     * remove a column grouper.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/Group)
     */
    groupFeature?: object | Boolean | string | Group | GroupConfig;
    /**
     * A special version of the Grid GroupSummary feature that enables summaries within scheduler. To use a single summary
     * it is easiest to configure [renderer](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/GroupSummary#config-renderer), for multiple summaries see [summaries](#Scheduler/feature/GroupSummary#config-summaries).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/GroupSummary)
     */
    groupSummaryFeature?: object | Boolean | string | GroupSummary | GroupSummaryConfig;
    /**
     * Right click column header or focus it and press SPACE key to show the context menu for headers.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/HeaderMenu)
     */
    headerMenuFeature?: object | Boolean | string | HeaderMenu | HeaderMenuConfig;
    /**
     * Enables users to click and drag to zoom to a date range in Scheduler's header time axis. Only supported in horizontal
     * mode.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/HeaderZoom)
     */
    headerZoomFeature?: object | Boolean | string | HeaderZoom | HeaderZoomConfig;
    /**
     * Displays labels at positions [top](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/Labels#config-top), [right](#Scheduler/feature/Labels#config-right), [bottom](#Scheduler/feature/Labels#config-bottom) and
     * [left](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/Labels#config-left).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/Labels)
     */
    labelsFeature?: object | Boolean | string | Labels | LabelsConfig;
    /**
     * This feature allows resources which satisfy a certain condition to be locked at the top of the scheduler.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/LockRows)
     */
    lockRowsFeature?: object | Boolean | string | LockRows | LockRowsConfig;
    /**
     * This feature merges cells that have the same value in sorted (or [optionally](https://bryntum.com/products/calendar/docs/api/Grid/feature/MergeCells#config-sortedOnly) any) columns
     * configured to [mergeCells](https://bryntum.com/products/calendar/docs/api/Grid/column/Column#config-mergeCells).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/MergeCells)
     */
    mergeCellsFeature?: object | Boolean | string | MergeCells | MergeCellsConfig;
    /**
     * Feature that allows styling of weekends (and other non-working time) by adding timeRanges for those days.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/NonWorkingTime)
     */
    nonWorkingTimeFeature?: object | Boolean | string | NonWorkingTime | NonWorkingTimeConfig;
    /**
     * Makes the scheduler's timeline pannable by dragging with the mouse. Try it out in the demo below.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/Pan)
     */
    panFeature?: object | Boolean | string | Pan | PanConfig;
    /**
     * Generates PDF/PNG files from the Scheduler component.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/export/PdfExport)
     */
    pdfExportFeature?: object | Boolean | string | PdfExport | PdfExportConfig;
    /**
     * Allows printing Scheduler contents using browser print dialog.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/export/Print)
     */
    printFeature?: object | Boolean | string | SchedulerPrint | SchedulerPrintConfig;
    /**
     * Feature that allows the user to search in a column by focusing a cell and typing. Navigate between hits using the
     * keyboard, [f3] or [ctrl]/[cmd] + [g] moves to next, also pressing [shift] moves to previous.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/QuickFind)
     */
    quickFindFeature?: object | Boolean | string | QuickFind | QuickFindConfig;
    /**
     * Makes the splitter between grid [sections](https://bryntum.com/products/calendar/docs/api/Grid/view/SubGrid) draggable, to let users resize, and
     * collapse/expand the sections.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/RegionResize)
     */
    regionResizeFeature?: object | Boolean | string | RegionResize | RegionResizeConfig;
    /**
     * Applicable only for Scheduler in `vertical` mode. Right click resource header cells to display a context menu.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/ResourceMenu)
     */
    resourceMenuFeature?: object | Boolean | string | ResourceMenu | ResourceMenuConfig;
    /**
     * Feature that draws resource time ranges, shaded areas displayed behind events. These zones are similar to events in
     * that they have a start and end date but different in that they do not take part in the event layout, and they always
     * occupy full row height.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/ResourceTimeRanges)
     */
    resourceTimeRangesFeature?: object | Boolean | string | ResourceTimeRanges | ResourceTimeRangesConfig;
    /**
     * Allow using [Ctrl/CMD + C/X] and [Ctrl/CMD + V] to copy/cut-and-paste rows. Also makes cut, copy and paste actions
     * available via the cell context menu.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/RowCopyPaste)
     */
    rowCopyPasteFeature?: object | Boolean | string | RowCopyPaste | RowCopyPasteConfig;
    /**
     * This feature allows editing of entire rows in a grid in a docked panel which by default slides out from the right.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/RowEdit)
     */
    rowEditFeature?: object | Boolean | string | RowEdit | RowEditConfig;
    /**
     * Enables expanding of Grid rows by either row click or double click, or by adding a separate Grid column which renders
     * a button that expands or collapses the row.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/RowExpander)
     */
    rowExpanderFeature?: object | Boolean | string | RowExpander | RowExpanderConfig;
    /**
     * This feature implements support for project transactions and used by default in Gantt. For general RowReorder feature
     * documentation see [RowReorder](https://bryntum.com/products/calendar/docs/api/Grid/feature/RowReorder).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/RowReorder)
     */
    rowReorderFeature?: object | Boolean | string | RowReorder | RowReorderConfig;
    /**
     * Enables user to change row height by dragging the bottom row border. After a resize operation, the [rowHeight](https://bryntum.com/products/calendar/docs/api/Grid/data/GridRowModel#field-rowHeight)
     * field of the record is updated (when [applyToAllRows](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/RowResize#config-applyToAllRows) is `false`) and the [eventLayout](#Scheduler/model/ResourceModel#field-eventLayout) of the dragged resource
     * row will be switched to `pack` as a result (only applies when `stack` is used by default).
     * `barMargin` and `resourceMargin` will also be shrunk as you reduce the height of a row.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/RowResize)
     */
    rowResizeFeature?: object | Boolean | string | RowResize | RowResizeConfig;
    /**
     * Allow visually selecting a schedule "cell" by clicking, or [any other pointer gesture](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/ScheduleContext#config-triggerEvent).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/ScheduleContext)
     */
    scheduleContextFeature?: object | Boolean | string | ScheduleContext | ScheduleContextConfig;
    /**
     * Displays a context menu for empty parts of the schedule. Items are populated in the first place
     * by configurations of this Feature, then by other features and/or application code.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/ScheduleMenu)
     */
    scheduleMenuFeature?: object | Boolean | string | SchedulerScheduleMenu | SchedulerScheduleMenuConfig;
    /**
     * Feature that displays a tooltip containing the time at the mouse position when hovering empty parts of the schedule.
     * To hide the schedule tooltip, just disable this feature:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/ScheduleTooltip)
     */
    scheduleTooltipFeature?: object | Boolean | string | SchedulerScheduleTooltip | SchedulerScheduleTooltipConfig;
    /**
     * This feature injects buttons to the beginning and end of each row that will scroll to the next or previous event
     * outside the current view. It can optionally show a label along with the button, using the
     * [labelRenderer](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/ScrollButtons#config-labelRenderer). Only supported in horizontal [mode](#Scheduler/view/SchedulerBase#config-mode)
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/ScrollButtons)
     */
    scrollButtonsFeature?: object | Boolean | string | ScrollButtons | ScrollButtonsConfig;
    /**
     * <div class="external-example " data-file="Grid/feature/Search.js"></div>
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/Search)
     */
    searchFeature?: object | Boolean | string | Search | SearchConfig;
    /**
     * Feature that displays a text field to edit the event name. You can control the flow of this by listening to the events relayed by this class from the underlying [Editor](https://bryntum.com/products/calendar/docs/api/Core/widget/Editor).
     * To use this feature, you also need to disable the built-in default editing feature:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/SimpleEventEdit)
     */
    simpleEventEditFeature?: object | Boolean | string | SimpleEventEdit | SimpleEventEditConfig;
    /**
     * Allows sorting of grid by clicking (or tapping) headers, also displays which columns grid is sorted by (numbered if
     * using multisort). Use modifier keys for multisorting: [Ctrl/CMD + click] to add sorter, [Ctrl/CMD + Alt + click] to remove sorter.
     * The actual sorting is done by the store, see [Store.sort()](https://bryntum.com/products/calendar/docs/api/Core/data/mixin/StoreSort#function-sort).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/Sort)
     */
    sortFeature?: object | Boolean | string | Sort | SortConfig;
    /**
     * This feature allows splitting the Scheduler into multiple views, either by using the cell context menu, or
     * programmatically by calling [split()](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/Split#function-split).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/Split)
     */
    splitFeature?: object | Boolean | string | Split | SplitConfig;
    /**
     * A feature which pins configurable content from a grid row to the top of the grid
     * while the row scrolls off the top but is still visible.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/StickyCells)
     */
    stickyCellsFeature?: object | Boolean | string | StickyCells | StickyCellsConfig;
    /**
     * This feature applies native `position: sticky` to event contents in horizontal mode, keeping the contents in view as
     * long as possible on scroll. For vertical mode it uses a programmatic solution to achieve the same result.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/StickyEvents)
     */
    stickyEventsFeature?: object | Boolean | string | StickyEvents | StickyEventsConfig;
    /**
     * Stripes rows by adding alternating CSS classes to all row elements (`b-even` and `b-odd`).
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/Stripe)
     */
    stripeFeature?: object | Boolean | string | Stripe | StripeConfig;
    /**
     * A special version of the Grid Summary feature. This feature displays a summary row in the grid footer.
     * For regular columns in the locked section - specify type of summary on columns, available types are:
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/Summary)
     */
    summaryFeature?: object | Boolean | string | Summary | SummaryConfig;
    /**
     * Adds scheduler specific menu items to the timeline header context menu.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/TimeAxisHeaderMenu)
     */
    timeAxisHeaderMenuFeature?: object | Boolean | string | TimeAxisHeaderMenu | TimeAxisHeaderMenuConfig;
    /**
     * Feature that renders global ranges of time in the timeline. Use this feature to visualize a `range` like a 1 hr lunch
     * or some important point in time (a `line`, i.e. a range with 0 duration). This feature can also show a current time
     * indicator if you set [showCurrentTimeLine](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/TimeRanges#config-showCurrentTimeLine) to true. To style the rendered elements, use the
     * [cls](https://bryntum.com/products/calendar/docs/api/Scheduler/model/TimeSpan#field-cls) field of the `TimeSpan` class.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/TimeRanges)
     */
    timeRangesFeature?: object | Boolean | string | SchedulerTimeRanges | SchedulerTimeRangesConfig;
    /**
     * Feature that allows selection of a time span in the time axis header. When a time span is selected in the header,
     * a [timeSelectionChange](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/TimeSelection#event-timeSelectionChange) event is fired.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/TimeSelection)
     */
    timeSelectionFeature?: object | Boolean | string | TimeSelection | TimeSelectionConfig;
    /**
     * Feature that makes the grid work more like a tree. Included by default in [TreeGrid](https://bryntum.com/products/calendar/docs/api/Grid/view/TreeGrid). Requires
     * exactly one [TreeColumn](https://bryntum.com/products/calendar/docs/api/Grid/column/TreeColumn) among grids columns. That column will have its renderer replaced with a
     * tree renderer that adds padding and icon to give the appearance of a tree. The original renderer is preserved and
     * also called.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/Tree)
     */
    treeFeature?: object | Boolean | string | Tree | TreeConfig;
    /**
     * A feature that allows transforming a flat dataset (or the leaves of a hierarchical) into a tree by specifying a
     * record field per parent level. Parents are generated based on each leaf's value for those fields.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Grid/feature/TreeGroup)
     */
    treeGroupFeature?: object | Boolean | string | TreeGroup | TreeGroupConfig;
    /**
     * A feature allowing you to roll up and display values in the time axis cell for each parent row in a tree scheduler.
     * ...
     * [View online docs...](https://bryntum.com/products/calendar/docs/api/Scheduler/feature/TreeSummary)
     */
    treeSummaryFeature?: object | Boolean | string | TreeSummary | TreeSummaryConfig;
};
